// main.js could handle client-side interactivity, like handling clicks or form submissions
document.addEventListener('DOMContentLoaded', () => {
    // Example: console log on document ready
    console.log('Document is ready!');

    // Further client-side scripting would go here
});
