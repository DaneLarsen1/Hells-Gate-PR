<div class="all-PRs-container">
  <div class="card-header">
    <h2>Feed</h2>
  </div>  

  <p>testing</p>

  {{#each lifts as |lift|}}
    <div class="lift">
      
      <p>
        {{lift.title}}
        {{lift.description}}
      </p>
    </div>
    <div class="col-md-7">
      <p>
        Posted by:
        {{lift.user_id}}
      </p>
    </div>
  {{/each}}
</div>